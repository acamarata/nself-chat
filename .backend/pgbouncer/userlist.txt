# PgBouncer User List
# Format: "username" "password"
# Password can be:
#   - Plain text
#   - MD5 hash: md5<md5 of password + username>
#   - SCRAM hash (PostgreSQL 10+)

# PostgreSQL superuser
"postgres" "postgres"

# Stats user (read-only for monitoring)
"stats" "stats123"

# Application user
"nchat_app" "nchat_password"

# Note: For production, use MD5 or SCRAM hashes instead of plain text
# Generate MD5 hash: echo -n "passwordusername" | md5sum | awk '{print "md5"$1}'
# Example MD5 entry:
# "postgres" "md5d8578edf8458ce06fbc5bb76a58c5ca4"
